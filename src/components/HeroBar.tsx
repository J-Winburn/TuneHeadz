/**
 * Alias for the app header — three-zone grid: logo (left), nav cluster (center), search (right).
 * Prefer importing `Header` or `HeroBar`; they are the same component.
 */
import Header from "./Header";

export default Header;
export const HeroBar = Header;
